{
  "name": "@openui/registry-schema",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Zod schemas, design.md parsing and validation for OpenUI registry items.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./node": "./src/node/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json",
    "lint": "eslint src",
    "test": "vitest run"
  },
  "dependencies": {
    "@openui/types": "workspace:*",
    "@openui/utils": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@openui/config": "workspace:*",
    "eslint": "^9.17.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
