/**
 * GENERATED by pnpm generate:mount-tests — do not edit.
 *
 * Mounts every previewable registry demo with the real renderer. A resource
 * that throws at import or at mount fails here with its own name.
 */
import React from "react";
import { describe, expect, it } from "vitest";

import Demo_BlueprintGrid from "@registry/default/backgrounds/blueprint-grid/demo";
import Demo_GrainBackground from "@registry/default/backgrounds/grain-background/demo";
import Demo_IndustrialDashboard from "@registry/default/blocks/industrial-dashboard/demo";
import Demo_AccordionIndex from "@registry/default/components/accordion-index/demo";
import Demo_CopyButton from "@registry/default/components/copy-button/demo";
import Demo_DockNavigation from "@registry/default/components/dock-navigation/demo";
import Demo_LedgerTable from "@registry/default/components/ledger-table/demo";
import Demo_MagneticButton from "@registry/default/components/magnetic-button/demo";
import Demo_MarqueeIndex from "@registry/default/components/marquee-index/demo";
import Demo_SegmentedControl from "@registry/default/components/segmented-control/demo";
import Demo_SmartBreadcrumb from "@registry/default/components/smart-breadcrumb/demo";
import Demo_StatusPill from "@registry/default/components/status-pill/demo";
import Demo_ToastRegion from "@registry/default/components/toast-region/demo";
import Demo_CursorTrail from "@registry/default/interactions/cursor-trail/demo";
import Demo_SpotlightFollow from "@registry/default/interactions/spotlight-follow/demo";
import Demo_AsymmetricSplit from "@registry/default/layouts/asymmetric-split/demo";
import Demo_ScrollReveal from "@registry/default/motion/scroll-reveal/demo";
import Demo_StickyStack from "@registry/default/motion/sticky-stack/demo";
import Demo_AsymmetricHero from "@registry/default/sections/asymmetric-hero/demo";
import Demo_EditorialNavigation from "@registry/default/sections/editorial-navigation/demo";
import Demo_BlurRevealText from "@registry/default/text/blur-reveal-text/demo";
import Demo_CircularType from "@registry/default/text/circular-type/demo";
import Demo_CounterTypography from "@registry/default/text/counter-typography/demo";
import Demo_DecryptText from "@registry/default/text/decrypt-text/demo";
import Demo_EditorialDropCap from "@registry/default/text/editorial-drop-cap/demo";
import Demo_EditorialHeading from "@registry/default/text/editorial-heading/demo";
import Demo_GradientOutlineType from "@registry/default/text/gradient-outline-type/demo";
import Demo_HoverReplaceText from "@registry/default/text/hover-replace-text/demo";
import Demo_KineticTicker from "@registry/default/text/kinetic-ticker/demo";
import Demo_ScrambleText from "@registry/default/text/scramble-text/demo";
import Demo_SplitText from "@registry/default/text/split-text/demo";
import Demo_TypeStack from "@registry/default/text/type-stack/demo";
import Demo_VariableWeightText from "@registry/default/text/variable-weight-text/demo";
import Demo_VerticalKernText from "@registry/default/text/vertical-kern-text/demo";
import Demo_WaveText from "@registry/default/text/wave-text/demo";

describe("registry demo mounts", () => {
  it("mounts blueprint-grid", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_BlueprintGrid)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts grain-background", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_GrainBackground)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts industrial-dashboard", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_IndustrialDashboard)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts accordion-index", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_AccordionIndex)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts copy-button", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_CopyButton)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts dock-navigation", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_DockNavigation)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts ledger-table", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_LedgerTable)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts magnetic-button", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_MagneticButton)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts marquee-index", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_MarqueeIndex)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts segmented-control", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_SegmentedControl)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts smart-breadcrumb", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_SmartBreadcrumb)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts status-pill", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_StatusPill)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts toast-region", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_ToastRegion)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts cursor-trail", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_CursorTrail)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts spotlight-follow", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_SpotlightFollow)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts asymmetric-split", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_AsymmetricSplit)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts scroll-reveal", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_ScrollReveal)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts sticky-stack", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_StickyStack)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts asymmetric-hero", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_AsymmetricHero)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts editorial-navigation", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_EditorialNavigation)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts blur-reveal-text", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_BlurRevealText)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts circular-type", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_CircularType)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts counter-typography", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_CounterTypography)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts decrypt-text", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_DecryptText)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts editorial-drop-cap", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_EditorialDropCap)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts editorial-heading", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_EditorialHeading)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts gradient-outline-type", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_GradientOutlineType)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts hover-replace-text", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_HoverReplaceText)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts kinetic-ticker", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_KineticTicker)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts scramble-text", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_ScrambleText)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts split-text", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_SplitText)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts type-stack", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_TypeStack)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts variable-weight-text", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_VariableWeightText)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts vertical-kern-text", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_VerticalKernText)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });

  it("mounts wave-text", async () => {
    const { createRoot } = await import("react-dom/client");
    const { act } = await import("react");
    const container = document.createElement("div");
    document.body.appendChild(container);
    const root = createRoot(container);
    await act(async () => root.render(React.createElement(Demo_WaveText)));
    expect(container.innerHTML).not.toBe("");
    await act(async () => root.unmount());
    container.remove();
  });
});
