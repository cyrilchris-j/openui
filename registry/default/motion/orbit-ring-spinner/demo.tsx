import { OrbitRingSpinner } from "./orbit-ring-spinner";

export default function Demo() {
  return (
    <div className="flex min-h-[16rem] items-center justify-center bg-paper p-8">
      <OrbitRingSpinner size={80} />
    </div>
  );
}
